import React from 'react';

import {Container, Title, Line} from './styles';

const Header = () => {
  return (
    <Container>
      <Title>Resgate</Title>
      <Line />
    </Container>
  );
};

export default Header;
